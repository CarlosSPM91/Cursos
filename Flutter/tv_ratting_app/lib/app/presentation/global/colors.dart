import 'package:flutter/material.dart';

class AppColors {
AppColors._();
static const primary = Color(0xff1565c0);
static const light = Color.fromARGB(255, 203, 235, 229);
static const dark = Color(0xff000a12);
static const darkLight = Color(0xff102027);

}